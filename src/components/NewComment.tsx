// import { CommentCard } from './CommentCard';

// type newCommentProps = {
//   message: string;
// };

// export const NewComment = ({ message }: newCommentProps) => {
//   return (
//     <CommentCard body={message} name={'Unknown user'} userName="unknown" />
//   );
// };
